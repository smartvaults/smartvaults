dictionary NostrConnectSession {
    NostrConnectURI uri;
    u64 timestamp;
};

interface NostrConnectRequest {
    EventId event_id();
    PublicKey app_public_key();
    string message();
    u64 timestamp();
    boolean approved();
};